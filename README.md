# 博利通企业展示站（初始化）

这是一个基于 **Vite + 原生 HTML/CSS** 的轻量级企业展示站初始化项目，适合快速搭建与后续迭代。

## 快速开始

```bash
npm install
npm run dev
```

默认开发地址：`http://localhost:5173`

## 可用脚本

- `npm run dev`：启动开发服务器
- `npm run build`：构建生产版本
- `npm run preview`：本地预览构建结果
